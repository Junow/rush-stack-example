
name: Release

on:
  push:
    branches: [ main ]

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v2
    - name: Setup Node.js
      uses: actions/setup-node@v1
      with:
        node-version: 12.18.0
    - name: install rush
      run: npm install -g @microsoft/rush
    - name: rush change
      run: rush change -v
    - name: rush install
      run: rush install --no-link
    - name: rush link
      run: rush link
    - name: rush build
      run: rush build --verbose
    - name: rush deploy
      run: rush deploy --overwrite -p @junow/rush-core
    - name: Add GiHub Package Token
      run: cd common/deploy/packages/core && echo "//npm.pkg.github.com/:_authToken=${{ secrets.GH_WEB_PACKAGES_TOKEN }}" > ~/.npmrc && ls && pwd
    - name: deploy @junow/rush-core
      run: npm publish
    - name: Add GiHub Package Token
      run: cd ../common && echo "//npm.pkg.github.com/:_authToken=${{ secrets.GH_WEB_PACKAGES_TOKEN }}" > ~/.npmrc
    - name: deploy @junow/rush-core
      run: npm publish
      env:
        NODE_AUTH_TOKEN: ${{ secrets.NODE_AUTH_TOKEN }}
